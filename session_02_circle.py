from math import pi #I think "math" is some kind of a library, which "pre?" stores information, like common knowledge to a certain topic (here: math), so now I got the book "pi" from the library "math" for my use?!
print(pi)

r = float(input("Enter radius a circle\n"))
print("The perimeter of the circle with radius ", r," cm is ", 2 * pi * r," cm.")
